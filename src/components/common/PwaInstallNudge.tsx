'use client';

/**
 * @deprecated Duplicate of PwaInstallPrompt — kept as a no-op so old imports don't crash.
 * Install UX is owned solely by `PwaInstallPrompt` in the root layout.
 */
export default function PwaInstallNudge() {
  return null;
}
