'use client';

import MarketingHeader from '@/components/marketing/system/MarketingHeader';

export default function PublicNavigation(_props: { onLoginClick?: () => void }) {
  return <MarketingHeader />;
}
