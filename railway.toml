[build]
builder = "NIXPACKS"
buildCommand = "rm -rf app/ .next/cache && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/readiness"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
