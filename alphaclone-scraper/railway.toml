# Railway service config for alphaclone-scraper.
# Railway dashboard: set service Root Directory to alphaclone-scraper.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
